# Archived UmgMcp Surface

이 디렉터리는 standalone `UmgMcp` MCP surface의 과거 문서와 Python server 자료를 보존한다.

현재 active Unreal MCP 진입점은 `UE_MCP_Bridge` 하나다. 여기 있는 `UmgMcpServer.py`, 55557 TCP listener, standalone setup 안내는 기본 실행 경로가 아니며 새 작업에서는 시작하지 않는다.
